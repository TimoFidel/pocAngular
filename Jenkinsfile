pipeline {
    agent any 
    stages {
        stage('Start app'){
    		steps{
    			bat 'npm install node-sass'
    		}
    	} 
        stage('Start app'){
    		steps{
    			bat 'ng serve'
    		}
    	}   

    }
}